services:
  app:
    image: ghcr.io/n3side/pautina_frontend:latest
    container_name: my-app
    ports:
      - "8080:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "host.docker.internal:host-gateway"